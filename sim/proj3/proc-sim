#!/usr/bin/env python
#=========================================================================
# proc-sim [options]
#=========================================================================
#
#  -h --help           Display this message
#
#  --impl              {ooo}
#  --input dataset     {vvadd-unopt,vvadd-opt,cmult}
#  --verify            Verify results
#  --trace             Display line tracing
#  --stats             Display statistics
#  --translate         Translate RTL model to Verilog
#  --dump-vcd          Dump VCD to proc-<impl>-<input>.vcd
#  --dump-vtb          Dump Verilog test bench
#  --short-mname       Use short module/testbench name
#  --max-cycles        Set timeout num_cycles, default=15000
#  --mem-delays        Turn on memory delays for testing
#
# Evaluation harness for the dual-issue OoO processor. System/cache and
# base-pipeline comparisons are intentionally out of scope for this branch.
#
# Author : Moyang Wang, Christopher Batten
# Date   : September 21, 2022
#

# Hack to add project root to python path

import os
import sys

sim_dir = os.path.dirname( os.path.abspath( __file__ ) )
while sim_dir:
  if os.path.exists( sim_dir + os.path.sep + "pymtl.ini" ):
    sys.path.insert(0,sim_dir)
    break
  sim_dir = os.path.dirname(sim_dir)

import argparse
import re

from random import randint, seed

seed(0xdeadbeef)

from pymtl3 import *
from pymtl3.stdlib.test_utils import config_model_with_cmdline_opts
from pymtl3.passes.backends.verilog import *

from proj3.ProcOoO             import ProcOoO

from proj3.test.harness        import TestHarness
from proj3.tinyrv2_encoding    import assemble

from proj3.ubmark.proc_ubmark_vvadd_unopt   import ubmark_vvadd_unopt
from proj3.ubmark.proc_ubmark_vvadd_opt     import ubmark_vvadd_opt
from proj3.ubmark.proc_ubmark_cmult         import ubmark_cmult


from proj3.ubmark.proc_ubmark_vmuladd2_unopt import ubmark_vmuladd2_unopt
from proj3.ubmark.proc_ubmark_chacha20_unrolled import ubmark_chacha20_unrolled
from proj3.ubmark.proc_ubmark_dotprod_unopt_unrolled import ubmark_dotprod_unopt_unrolled
from proj3.ubmark.proc_ubmark_vvadd_sw_opt import ubmark_vvadd_sw_opt

#-------------------------------------------------------------------------
# Command line processing
#-------------------------------------------------------------------------

class ArgumentParserWithCustomError(argparse.ArgumentParser):
  def error( self, msg = "" ):
    if ( msg ): print("\n ERROR: %s" % msg)
    print("")
    file = open( sys.argv[0] )
    for ( lineno, line ) in enumerate( file ):
      if ( line[0] != '#' ): sys.exit(msg != "")
      if ( (lineno == 2) or (lineno >= 4) ): print( line[1:].rstrip("\n") )

def parse_cmdline():
  p = ArgumentParserWithCustomError( add_help=False )

  # Standard command line arguments

  p.add_argument( "-h", "--help",                    action="store_true" )

  # Additional commane line arguments for the simulator

  p.add_argument( "--impl",  default="ooo", choices=["ooo"] )
  p.add_argument( "--input", default="vvadd-unopt",
    choices=["vvadd-unopt", "vvadd-opt", "cmult", "vmuladd2-unopt", "chacha20-unrolled", "dotprod-unopt-unrolled", "vvadd-sw-opt"] )

  p.add_argument( "--trace",                     action="store_true" )
  p.add_argument( "--stats",                     action="store_true" )
  p.add_argument( "--verify",                    action="store_true" )
  p.add_argument( "--dump-vcd",                  action="store_true" )
  p.add_argument( "--translate",                 action="store_true" )
  p.add_argument( "--dump-vtb",                  action="store_true" )
  p.add_argument( "--short-mname",               action="store_true" )
  p.add_argument( "--mem-delays",                action="store_true" )
  p.add_argument( "--max-cycles", default=15000, type=int            )

  opts = p.parse_args()
  if opts.help: p.error()
  return opts

#-------------------------------------------------------------------------
# Main
#-------------------------------------------------------------------------

def get_commit_count( th ):

  if hasattr( th, "proc" ) and hasattr( th.proc, "commit_count" ):
    return int( th.proc.commit_count )

  if hasattr( th, "commit_count" ):
    return int( th.commit_count )

  return 1 if th.commit_inst else 0

def main():
  opts = parse_cmdline()

  # Create test harness (we can reuse the harness from unit testing)

  th = TestHarness( ProcOoO )

  # Set the memory latency

  if opts.mem_delays:
    th.set_param( "top.mem.construct", stall_prob=0.5 )
    th.set_param( "top.mem.construct", extra_latency=3 )

  # Create VCD/VTB filename

  unique_name = f"proc-{opts.impl}-{opts.input}"

  if opts.short_mname:
    vtb_name = f"{opts.input}"
  else:
    vtb_name = unique_name

  cmdline_opts = {
    'dump_vcd'    : f"{unique_name}" if opts.dump_vcd else '',
    'dump_vtb'    : f"{vtb_name}"    if opts.dump_vtb else '',
    'test_verilog': 'zeros'          if opts.translate else '',
  }

  # Configure the test harness component

  th.elaborate()

  config_model_with_cmdline_opts( th, cmdline_opts, duts=['proc'] )

  # Apply necessary passes

  th.apply( DefaultPassGroup( linetrace=opts.trace ) )

  # Determine input program

  input_dict = {
    "vvadd-unopt"   : ubmark_vvadd_unopt,
    "vvadd-opt"     : ubmark_vvadd_opt,
    "cmult"         : ubmark_cmult,

    "vmuladd2-unopt": ubmark_vmuladd2_unopt,
    "chacha20-unrolled": ubmark_chacha20_unrolled,
    "dotprod-unopt-unrolled": ubmark_dotprod_unopt_unrolled,
    "vvadd-sw-opt"  : ubmark_vvadd_sw_opt,
  }

  input = input_dict[ opts.input ]

  # Assemble the test program

  mem_image = input.gen_mem_image()

  # Load the program into the model

  th.load( mem_image )

  # Reset test harness

  print()
  th.sim_reset()

  # Run simulation

  num_insts = 0
  while not th.done() and th.sim_cycle_count() < opts.max_cycles:
    th.sim_tick()

    # Count retired instructions, including two-wide commit cycles.
    num_insts += get_commit_count( th )

  # Force a test failure if we timed out

  num_cycles = th.sim_cycle_count()
  assert num_cycles < opts.max_cycles

  # Extra ticks to make VCD easier to read

  th.sim_tick()
  th.sim_tick()
  th.sim_tick()

  if opts.verify:
    passed = input.verify( th.mem.mem.mem )
    print()
    if not passed:
      exit(1)

  if opts.stats:
    cpi = float(num_cycles) / float(num_insts) if num_insts > 0 else 0.0
    print( " num_cycles = {}".format( num_cycles ) )
    print( " num_insts  = {}".format( num_insts ) )
    print( " CPI        = {:1.2f}".format( cpi ) )
    print()

main()
